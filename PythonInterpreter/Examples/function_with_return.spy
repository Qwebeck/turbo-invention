def square(arg1):
    return arg1 * arg1
end

def foo(a, b):
    a = square(a)
    b = square(b)
    if a > b:
        print("a is greater than b", a, b)
    else:
        print("b is greater than a", b, a)
    end
end

foo(2, 3)
